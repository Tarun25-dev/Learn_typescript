let n: number| string = "tharun";

if (typeof n === "string"){
    console.log(n.toUpperCase());
}

let m: number | string = 25;

if (typeof m === "number"){
    console.log(m.toFixed(2)); // here 2 means after that number keep two decimal numbers .00
}
