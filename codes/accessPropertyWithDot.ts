// Use . when you know the property name directly.

const user = {
    name: " tharun",
    age: 22,
    city: "nandyal"
};

// here i know exactly what property i want 

console.log(user.name);
console.log(user.age);
console.log(user.city);